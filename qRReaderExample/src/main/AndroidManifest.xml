<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qr_readerexample"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />
    
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".views.DecoderActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".views.VideoActivity"
            android:label="Tcc 2 - Luan">
        </activity>

        <activity
            android:name=".views.VideoSupino"
            android:label="Tcc 2 - Luan">
        </activity>

        <activity
            android:name=".views.Videoleg"
            android:label="Tcc 2 - Luan">
        </activity>

        <activity
            android:name=".views.VideoTriceps"
            android:label="Tcc 2 - Luan">
        </activity>

        <activity
            android:name=".views.VideoPuxada"
            android:label="Tcc 2 - Luan">
        </activity>

        <provider
            android:authorities="com.example.qr_readerexample.common.providers.myprovider"
            android:name=".common.providers.MyProvider"/>
    </application>

</manifest>